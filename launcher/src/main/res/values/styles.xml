<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="module_launcher_AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="Fullscreen_Dialog">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@style/Animation</item>
    </style>

    <style name="Fullscreen_Check" parent="Fullscreen_Dialog">
        <item name="android:windowBackground">@color/module_sub_color_black</item>
    </style>

    <style name="Fullscreen_Sub" parent="Fullscreen_Dialog">
        <item name="android:windowBackground">@drawable/launcher_dialog_sub</item>
    </style>

    <style name="Fullscreen_QRCode" parent="Fullscreen_Dialog">
        <item name="android:windowBackground">@color/module_sub_color_black</item>
    </style>

    <style name="Animation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>
</resources>
